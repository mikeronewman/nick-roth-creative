<header class="header">
    <div class="wrapper">
        <h2 class="header__title">Nick Roth | Creative</h2>
        
        <nav>
            <a class="button header__contactbtn" href="#">CONTACT</a>

            {% set navPages = collections.all | eleventyNavigation %}
            {%- for entry in navPages %}

                <a class="header__desktoplinks display-largescrn" href="{{ entry.url | url }}">{{ entry.title }}</a>

            {%- endfor %}

            <div class="hamburger hidden-largescrn">
                <span></span>
                <span></span>
                <span></span>
            </div>

        </nav>
    </div>
</header>